import { defineConfig } from 'vite';


export default {
    base: '/login-project/', // Replace 'repository-name' with your GitHub repository name
    build: {
        outDir: 'dist', // Output directory for build files
      },
  };
  